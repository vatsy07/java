class Solution {
public:
    int arrayPairSum(vector<int>& nums) {
        int n = nums.size();

        sort(nums.begin(), nums.end());

        int x = n / 2;
        int sum = 0;

        for (int i = 0; i < x; i++) {
            sum = sum + nums[2 * i];
        }

        return sum;
    }
};
